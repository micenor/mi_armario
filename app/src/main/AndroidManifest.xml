<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.angel.mi_armario" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_miarmario"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_miarmario_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Splash"
            android:exported="true"
            android:theme="@style/AppTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PantallaPrincipal"
            android:exported="false" />
        <activity
            android:name=".PantallaRegistroLogin"
            android:exported="false" />
        <activity
            android:name=".PantallaRegistrarse"
            android:exported="false" />
    </application>

</manifest>